# Demo Video

Place the 30-second Cloud Run demo recording here as:

```text
ticketrouter-cloudrun-demo.mp4
```

Recommended flow:

1. Open the live API root endpoint.
2. Open `/health`.
3. Run `POST /route` from Cloud Shell.
4. End on the Run 2 accuracy summary.
